{{- if .Values.configFile.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webapp.name" . }}-config
  labels: {{- include "webapp.labels" . | nindent 4 }}
data:
  {{ .Values.configFile.subPath }}: |
    {{- .Values.configFile.content | nindent 4 }}
{{- end }}
