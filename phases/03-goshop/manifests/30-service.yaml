apiVersion: v1
kind: Service
metadata:
  name: goshop
  namespace: goshop
spec:
  type: NodePort
  selector:
    app: goshop
  ports:
    - name: http
      port: 8888
      targetPort: http
      nodePort: 30088
    - name: grpc
      port: 8889
      targetPort: grpc
      nodePort: 30089
