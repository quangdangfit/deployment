apiVersion: v1
kind: Service
metadata:
  name: {{ include "goshop.api.fullname" . }}
  labels: {{- include "goshop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.api.type }}
  selector: {{- include "goshop.api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.api.httpPort }}
      targetPort: http
    - name: grpc
      port: {{ .Values.service.api.grpcPort }}
      targetPort: grpc
